# Supabase PostgreSQL
POSTGRES_URL=

# Better Auth — generate secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Vercel AI Gateway — https://vercel.com/docs/ai-gateway
VERCEL_AI_GATEWAY=

# Mailgun — email verification OTP
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_FROM=

# Optional: Vercel Blob for file uploads
BLOB_READ_WRITE_TOKEN=
